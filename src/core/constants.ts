// Настройки поведения ядра — собраны в одном месте, чтобы легко крутить.

/** «Период благодати»: сколько миллисекунд закрытый пункт живёт зачёркнутым. */
export const GRACE_MS = 6000

/** «Горит»: срок наступит в пределах этого окна (или уже просрочен). 12 часов. */
export const SOON_MS = 12 * 60 * 60 * 1000

/** «Пора пнуть»: ожидание, чей срок в пределах этого окна. 3 дня. */
export const NUDGE_MS = 3 * 24 * 60 * 60 * 1000

/** Ожидание без срока считается «пора пнуть», если висит дольше этого. 2 дня. */
export const STALE_MS = 2 * 24 * 60 * 60 * 1000

/** На сколько «Отметить напомнил» сдвигает дату следующего касания. 3 дня. */
export const REMIND_SNOOZE_MS = 3 * 24 * 60 * 60 * 1000
